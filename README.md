# model-persona-drift
How much does a model’s projection onto the Assistant Axis change under prompt perturbations? This repo contains an experiment where I evaluate how simple perturbations cause a model's persona to drift.
